before_build:
  - nuget restore

after_build:
- ps: nuget pack src\UrlFilter\UrlFilter.csproj

deploy:
  provider: NuGet
  api_key:
    secure: /CRVOWwBjL+5HdnNQlKNu049zushOyHsoG/3weq7Re7/msT+z2/hXABNtkdBTkVb
  skip_symbols: false

artifacts:
  - path: '*.nupkg'

version: 1.0.{build}
assembly_info:
  patch: true
  file: '**\AssemblyInfo.*'
  assembly_version: '{version}'
  assembly_file_version: '{version}'
  assembly_informational_version: '{version}'
nuget:
  project_feed: true
build:
  publish_nuget: true
  publish_nuget_symbols: true
  include_nuget_references: true
  verbosity: normal